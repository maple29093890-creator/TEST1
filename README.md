# TEST1
## TEST1
### TEST1
~~#### TEST1~~

TEST1
```c
#include <studio.h>
 if(ch == '\n')
       HAL_UART_Transmit (&huart2, (uint8_t*) "\r", 1, 0xFFFF);
   HAL_UART_Transmit (&huart2, (uint8_t*) &ch, 1, 0xFFFF);

```
